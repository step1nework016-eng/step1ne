# Step1ne 前端網站規則

靜態站台，`git push deploy HEAD:main` 部署（remote 固定叫 `deploy`，不是 `origin`——`origin` 是沒接 webhook 的舊備份，推過去不會上線）。

## jobs/index.html 是高風險檔案，改之前先讀這段

`publish_job.py` 的 `update_list()` 函式會用 `class="job"` 選擇器直接改寫這個檔案——**這支函式本身有已知未修的 bug**（曾經造成職缺卡片重複 128 次的真實事故）。除非你已經重新驗證過這支函式的插入/取代邏輯，否則：

- 不要跑 `publish_job.py` 去改這個檔案的排序或內容
- 手動改職缺卡片順序/內容，用精準的文字擷取＋回寫（每張卡片是完整的 `<a class="job" href="/jobs/<slug>/">...</a>` 區塊），改完務必驗證：卡片數量不變、每張卡片內容逐字比對沒有被誤改，只有位置變了才算安全
- 改完先看 `git diff --stat`，一個排序/加減幾張卡片的改動，diff 行數應該跟改動量成比例——如果一次改動跳出幾百行的 diff，先停下來，不要 push

## 手機版篩選 UI

篩選器/分類 tab 在手機版一律用原生 `<select>`，桌機保留橫排 chip 按鈕——主導覽跟只有 2–3 個選項的不適用這條，純粹是「選項一多，橫排 chip 在小螢幕上點不準」這個具體問題的解法。

## 資源驗證

改完頁面，不能只看標籤存在，要實際確認每個圖片/CSS 檔案回 200（不是只有 HTML 結構正確）。子目錄頁面的資源路徑一律用絕對路徑（`/assets/...`），本機測試的路徑深度要跟線上一致，不然本機測得過、線上 404。

## 設計風格

現有站台是暖米色系（`--bg:#f4f1ea`、金色 `--gold:#a67c3d` 點綴）。這個配色本身已經很接近 AI 生成設計常見的「暖米色+襯線字+赭色點綴」樣板，**不要在既有頁面基礎上再往這個方向加深**；新增獨立頁面（如 `consultant/`、`jobs/` 之外的全新功能頁）可以考慮不同的視覺方向，避免整個網域看起來像同一套 AI 預設模板。手機 RWD、鍵盤 focus 可見、`prefers-reduced-motion`、對比 4.5:1、點擊目標 44×44px 是底線，不用特別提醒也要做到。

## 部署後驗證

CF Pages 部署有 30–90 秒延遲，deploy 完不要連續補推第二個 commit「順便修一下」——會讓 build 重新排隊，反而拖更久看到結果。用瀏覽器工具驗證時，先確認頁面原始碼裡有新程式碼的關鍵字（例如 `fetch` 一段獨特字串比對），再判斷是不是快取問題還是真的沒部署成功。
