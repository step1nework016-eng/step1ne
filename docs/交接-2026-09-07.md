# Step1ne 官網　2026-09-07 交接

## 已上線（都驗證過線上實際狀態）

| commit | 內容 |
|---|---|
| `e20b2b1` | 修正 35 個職缺頁的 JobPosting schema（年薪誤標月薪、幣別錯、地點缺、僱用型態用中文值） |
| `6057cf1` | 31 篇文章換版面＋文章列表改雙受眾×旅程階段＋sitemap 補回漏掉的 7 篇 |
| `d82adda` | 修文章列表頁 logo 相對路徑 404 |
| `572600a` | 對外頁面移除委任客戶名＋職缺列表補完 28 張空白卡片、修正分類 |

要還原任何一個：`git revert <commit> && git push deploy main`

## 還在本地、沒上線

- `redesign-home.html`　新版首頁（含 A 級評鑑區塊、進度軌、阿財介紹、完整頁尾）
- `redesign-jobs.html`　新版職缺頁（分組薪資尺、手機下拉）
- `缺圖清單.html`　25 張待生成的文章配圖，含可直接複製的提示詞
- `docs/bim-keyword-analysis-2026-09-07.html`　BIM 關鍵字分析
- `docs/figs-variants.html`　首頁數字區的五種做法比較
- `docs/seo-baseline-2026-09-07.md`　SEO 基準與回收日

預覽：`python3 -m http.server 8932 --directory .` 然後開 http://localhost:8932/

## 待決定（都不急，放著不會壞）

### 1. 兩組同名職缺，要關掉哪一筆
律准自己撞自己，兩組後台都還是 open。在關掉之前，阿財有拿錯 JD 的風險。

| 職稱 | A（2026-08-26） | B（2026-09-03 後台上架） |
|---|---|---|
| 資深職業安全衛生工程師 | `/jobs/ehs-engineer-yunlin/` 月薪 4 萬起 | `/jobs/pending-co_802bdd6b-3b807a75/` 月薪 5–6 萬 |
| 資深工程設計工程師 | `/jobs/engineering-design-engineer-hsinchu/` 月薪 4 萬起 | `/jobs/pending-co_802bdd6b-dd0123c6/` 月薪 5–6 萬 |

A 的優勢：網址語意清楚、內容較完整、已被 Google 收錄。
B 的優勢：薪資較新較高。

### 2. 6 筆 `pending-co_802bdd6b-*` 暫存網址要不要轉正
已被 Google 收錄、社群貼文與應徵連結指向它們，轉正必須做 301。

### 3. 新版首頁與職缺頁要不要上線
這兩頁會改 H1（首頁「為關鍵職位…」→「找人這件事，應該看得到進度」；
職缺「職缺專區」→「N 個開放中的職缺」），會同時影響排名與 CTR，
要單獨記基準、單獨回收，不可和 sitemap 那輪混在一起。

## 回收日

**2026-10-05**　看 sitemap 補進去的 7 篇文章有沒有拿到曝光（判準：至少 4 篇 > 0）。
基準數字在 `docs/seo-baseline-2026-09-07.md`。

## 注意

- 另一個 session 的分支 `claude/vigorous-wright-5ead7a`（commit `6b0a250`）也改了
  `jobs/index.html`，跟 main 會衝突，要 rebase。它有一支可重複執行的
  `scripts/gen_job_cards.py`，那是根本解法，值得保留並改成產生目前 main 的內容。
- 顧問後台上架職缺的缺陷記在記憶檔 `step1ne_job_backend_defects.md`
